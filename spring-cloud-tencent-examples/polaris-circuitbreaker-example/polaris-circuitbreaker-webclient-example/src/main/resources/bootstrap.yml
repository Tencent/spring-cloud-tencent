server:
  port: 48080
spring:
  application:
    name: polaris-circuitbreaker-webclient-example
  cloud:
    polaris:
      address: grpc://14.22.2.171:8091
      namespace: default
      enabled: true
      loadbalancer:
        enabled: true
      circuitbreaker:
        enabled: true

logging:
  level:
    root: info
    com.tencent.cloud: debug

